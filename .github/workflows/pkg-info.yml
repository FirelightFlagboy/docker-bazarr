name: Update pkg info file

on:
  schedule:
    - cron: 0 5 1 * * # every 1st day of the month
  workflow_dispatch:
  pull_request:
    paths:
      - .github/workflows/pkg-info.yml

jobs:
  update-pkg:
    uses: FirelightFlagboy/gh-actions-workflows-docker-services/.github/workflows/update-pkg-info.yml@40e0da522f9997f3a57903cb0285446434d8fe9e
    permissions:
      contents: write
      pull-requests: write
    with:
      pkg-info-path: pkg-info.json
